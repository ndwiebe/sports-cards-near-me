# Sports Cards Near Me

A mobile-friendly directory of sports card stores across Canada, with locations, hours, and map search. 570+ stores and counting.

## How it works

Plain HTML + JavaScript + Tailwind CSS, with Google Maps for search and store pins. Store data lives in a Google Sheet and the site rebuilds from it daily, so keeping the directory current is a spreadsheet edit, not a code change.
