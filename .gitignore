node_modules/
dist/
.astro/
test-results/
playwright-report/
.DS_Store
.env*
genimg-out/

# NOTE: ratings-refresh CSVs are deliberately NOT ignored. The monthly
# ratings-refresh workflow opens a PR containing them, and reviewing those rows
# in the diff is the point — an ignore here made that PR come out empty.

__pycache__/

# Claude Code local settings (machine-specific, never committed)
.claude/settings.local.json

# wrangler's local cache/state (created by running any `wrangler` command in this
# worktree, e.g. scripts/click-report.py). Confirmed 2026-09-03: this is also the empty
# local KV store that `wrangler kv key list` silently reads from instead of Cloudflare
# when --remote is omitted -- machine-local scratch, never something to commit.
.wrangler/
