# Plan: Q4 Phase 1b — round 2 titles/descriptions + Pokémon noindex

Scope: PRD `docs/PRD-q4-2026-growth.md` Phase 1b (round 2 title rewrite) and the
Phase 3 Pokémon noindex bullet. Lane: pages/lib, worktree `scnm-q4-titles`.
Files touched stay inside `src/lib/seo.ts`, `src/lib/tcg.ts`,
`src/lib/store-hours.ts`, `src/pages/[province]/**`, `src/pages/pokemon/**`,
`src/pages/guides/{card-grading-companies-canada,best-card-shops-calgary,best-card-shops-edmonton}.astro`,
`astro.config.mjs`, `tests/unit/**`, `docs/research/**`. Not touched:
`src/lib/shows.ts`, show pages, `scripts/refresh-*`, sheet writes, about/privacy/FAQ copy.

## 1. Measure round 1

Round 1 = `8bff32da` (store titles + false "Top rated" fix) and `77317e47`
(city/province/shows/sell/pokemon/home template rewrite, 2026-08-07 — this is
the commit the PRD's "round 1" points to for city/province/Pokémon copy).

`scripts/fetch-gsc.py` needs a live logged-in Chrome on debug port 9222 —
confirmed unreachable in this environment (`curl 127.0.0.1:9222` refused).
Fall back to the two Search Console exports at
`/Users/nathanwiebe/.claude/uploads/d4a177a3-4de0-4e36-82b2-a3f839bf7412/*.xlsx`.
The Performance export covers 2026-07-28 → 2026-09-21 as **page-level totals for
the whole window** — no per-page daily breakdown, so no true before/25-Aug
vs. after/25-Aug split is possible page-by-page. Report this limitation
explicitly. Output: `docs/research/2026-09-23-title-round1-ctr.md`.

## 2. Round 2 titles/descriptions, built from real data

Add to `src/lib/seo.ts`:
- `openSundayCount(stores)` — count of stores whose parsed hours include Sunday
  (uses `parseStoreHours` from `store-hours.ts`).
- `cityTitle(city, province, stores, noneOpen)` / `cityDescription(...)` —
  replace the inline ternaries in `src/pages/[province]/[city]/index.astro`.
  Keep round 1's absence-of-data rules (no claim a lone/crownless/no-rating
  city can't back) and the superlative-claims guard (`tests/unit/superlative-claims.test.ts`
  bans "top/best/highest-rated" — use "ranks first" / "highest-ranked" per its
  own docstring). Add the Sunday-open count and "near you" wording where real.
- `provinceTitle` / `provinceDescription` for `src/pages/[province]/index.astro`,
  same shape one level up.

Guides (`card-grading-companies-canada`, `best-card-shops-calgary`,
`best-card-shops-edmonton`): hand-edit the existing `title`/`description`
consts to fold in a real number already computed on the page (shop counts,
grading-shop counts) — these are bespoke content pages, not template-driven,
so no seo.ts helper for them.

Tests: extend `tests/unit/page-titles.test.ts` (new `cityTitle`/`cityDescription`/
`provinceTitle`/`provinceDescription` describes) and `tests/unit/store-hours.test.ts`
(`openSundayCount`/`openOnSunday`). Keep the existing store-title and
`rankedFirstPhrase` tests' intent unchanged.

## 3. Pokémon: noindex single-shop city pages + round 2 copy

- `src/lib/tcg.ts`: `MIN_POKEMON_SHOPS_TO_INDEX = 2`, `pokemonCityTooThinToIndex(count)`,
  mirroring `resellersTooThinToIndex` in `src/lib/resellers.ts`. Plus
  `pokemonCityTitle`/`pokemonCityDescription` reusing the same real-data
  pattern as `cityTitle`.
- `src/pages/pokemon/[city]/index.astro`: `noindex={pokemonCityTooThinToIndex(shops.length)}`
  on `<Base>`, never hardcoded — same shape as `resellers/index.astro`.
- `astro.config.mjs`: extend the sitemap `filter` with a `THIN_POKEMON_CITY_PATHS`
  set computed from `stores.json` (grouped by `citySlug`, count < 2), same
  pattern as `CLOSED_STORE_PATHS`.
- Test: `tests/unit/tcg.test.ts` (or new `pokemon-indexing.test.ts`) mirroring
  `resellers-indexing.test.ts` — thin/indexable boundary, the page source
  asks Base for a conditional noindex, and the sitemap-filter threshold
  matches the lib constant.

## Verification

Before every commit: `npm run typecheck && npm test`. Before the final commit
also `npm run build`, compare page count to baseline 1548 (noindexed pages
still build — count should be unchanged), and grep the built sitemap to
confirm noindexed Pokémon single-shop pages are absent from it while still
serving 200. Screenshot two changed pages at 375×812 with Playwright to
`/private/tmp/claude-501/.../scratchpad/titles-*.png`.
